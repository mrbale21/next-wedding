export const dynamic = "force-dynamic";

import SectionPage from "./section/page";

export default function Page() {
  // Untuk sementara, kita tidak gunakan searchParams sama sekali
  return <SectionPage />;
}
